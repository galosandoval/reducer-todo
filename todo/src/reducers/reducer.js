const { useReducer } = require("react");

const initialValues = {
  item: "Learn about reducers",
  completed: false,
  id: 3892987589,
};

reducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
